//not really connected to mongo
/*
const mongoose = require('mongoose')
const Schema = mongoose.Schema;

const PostModels = new Schema ({
    title: String,
    body: String
});

const Models = mongoose.model('Model', PostModels);

module.exports = Models
*/
