import React from 'react'
import Controller from '../controller/controller'

export default function Home() {
    return (
        <Controller.Button
        text = "Add Task +"
        variant = "outlined"
        />
    )
}
